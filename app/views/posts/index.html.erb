<% @post.each do |post| %>
  <div class="alert">
    <h3><%= post.title %></h3>
    <p><%= post.description %></p>
    <%= link_to "View in Full", post_path(post), :class => "btn btn-primary" %>
  </div>

<% end %>